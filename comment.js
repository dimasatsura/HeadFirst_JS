var price = 28.99;//создание переменой price и присваивание ей значение 28.99
var discount = 10;// создание переменной discount и присваивание ей значения 10 
var total = // создание переменной total 
    price - (price * (discount / 100));//присваивание переменной total результатов вычисления 
if (total > 25) { /*сравнение переменной total and 25,
     если total больше то выполняется freeShipping*/ 
    freeShipping();//выполнениие кода  
} // конец команды if

var count = 10;// присвайвание переменной count значения 10 
while (count > 0) {// count больше нуля й   
    juggle();// выполнение
count = count - 1;// count - 1 
 }// конец команы while

var dog = {name: "Rover", weight: 35};// создание переменной dog с атрибутами name and weight 
    if (dog.weight > 30) {// если атрибут weight больше 30
alert("WOOF WOOF"); }// то WOOF WOOF  
    else {// если меньше 30
    alert("woof woof");// то woof woof
}// конец команыды if

var circleRadius = 20;// присванивание переменной значения 20
var circleArea = // создание переменной  
Math.PI * (circleRadius * circleRadius);// и привавание ей вычисления 